# JaceCanvas 开源版使用、配置与打包说明

> 当前源码基于 JaceCanvas 3.11.6 整理。开源版是通用客户端，不绑定任何个人主控平台、ComfyUI 实例或私有工作流。

> **源码开放声明**：JaceCanvas 自有画布、节点运行时、界面和通用适配代码按本目录 `LICENSE` 发布。任何人都可以在遵守许可证的前提下修改画布、开发新节点、制作衍生版本和二次开发；发布衍生版本时请保留版权与许可证说明，并明确标注自己的修改。

## 开源组件、超分/补帧节点与版权边界

### Video2X 节点功能

画布内置 `Video2X 超分/补帧` 节点，支持：

- 视频超分：RealESRGAN AnimeVideo、RealESRGAN Plus、RealCUGAN；
- 视频补帧：RIFE，可选 2 倍或 4 倍帧率；
- 超分 + 补帧串联处理；
- 本地视频选择、结果预览和连接到下一个画布节点。

节点适配代码由 JaceCanvas 提供；Video2X Qt6 可执行文件、RealESRGAN/RealCUGAN/RIFE 模型、FFmpeg、Qt/NCNN/Vulkan 运行库均是第三方内容。节点界面明确标注“开源本地模型”，并提示可能不适配所有电脑。

### 来源和协议说明

| 内容 | 来源 | 许可边界 |
|---|---|---|
| Video2X Qt6 | [`k4yt3x/video2x`](https://github.com/k4yt3x/video2x) | 以 Video2X 上游仓库当前 LICENSE 为准，重新分发须保留上游声明 |
| Real-ESRGAN / RealCUGAN | 上游项目及其模型发布页 | 程序和模型可能有不同许可证，分别核对后再分发 |
| RIFE | 上游项目及模型发布页 | 依版本和模型文件的许可证执行 |
| FFmpeg、Qt、NCNN、Vulkan 等 | 各自上游项目 | 以随附版权/许可证文件为准 |
| JaceCanvas 节点适配和画布代码 | 本项目 | 以本目录 MIT License 为准 |

仓库默认不提交第三方 Video2X 运行时和模型，避免未经审核的大文件、许可证和再分发问题。安装包或 Release 若包含它们，必须同时提供对应的版权/许可证文件；本项目不把第三方程序或模型称为自研，也不替用户授予商业授权。

用户必须自行确认：输入素材、输出内容、模型、驱动和第三方组件的使用权；禁止侵犯他人著作权、商标权、肖像权、隐私权或其他合法权益。

完整清单见 [`THIRD_PARTY_NOTICES.md`](./THIRD_PARTY_NOTICES.md)。

## 一、目录说明

```text
开源/
├─ 开源使用说明.md       # 本文档
├─ README.md             # 开源包入口说明
├─ icon1.ico             # 构建使用的图标
├─ desktop-app/          # Electron + React + Vite 桌面端源码
└─ server/               # Express + SQLite 本地数据服务源码
```

本开源包没有包含 `node_modules`、`dist`、安装包、数据库和日志。第一次使用需要安装依赖并构建。

## 二、环境要求

- Windows 10/11 64 位
- Node.js 18 或更高版本，推荐 Node.js 20 LTS
- npm 9 或更高版本
- 8 GB 以上内存，推荐 16 GB 以上
- 支持 WebGL 2.0 的显卡
- 如果要使用 SSH 性能监控，需要目标服务器开放 SSH，并在应用设置中填写信息

## 三、第一次配置

### 1. 安装桌面端依赖

在 PowerShell 中执行：

```powershell
cd E:\AIhuabu\JaceCanvas\开源\desktop-app
npm install
```

### 2. 安装本地服务端依赖

```powershell
cd E:\AIhuabu\JaceCanvas\开源\server
npm install
```

### 3. 配置提示词优化器（可选）

复制模板：

```powershell
cd E:\AIhuabu\JaceCanvas\开源\desktop-app
Copy-Item config\prompt-settings.example.json config\prompt-settings.json
```

然后根据自己的服务修改：

```json
{
  "optimizerProvider": "openai",
  "optimizerBaseUrl": "https://api.openai.com/v1",
  "optimizerApiKey": "",
  "optimizerModel": "gpt-4o-mini"
}
```

注意：`prompt-settings.json` 只在本机使用，不能提交到公共仓库。也可以不创建这个文件，直接在应用设置中配置。

## 四、配置自己的主控平台

JaceCanvas 的工作流节点来自用户自己的主控平台。每个人的节点列表、工作流名称、参数、模型和可用能力都可能不同。

启动应用后，打开设置，填写：

| 配置项 | 填写内容 |
|---|---|
| API 地址 | 主控服务根地址，不要附加 `/api` 或具体接口路径 |
| API 密钥 | 主控要求认证时填写；不需要则留空 |
| ComfyUI 地址 | 用于打开或测试原生 ComfyUI 页面，可选 |
| SSH 主机/端口/用户/密码 | 仅用于远程性能监控，可选 |

示例格式：

```text
API 地址：https://your-control.example.com:8443
ComfyUI 地址：https://your-comfy.example.com:8443
SSH 主机：gpu.example.com
SSH 端口：22
SSH 用户：your-user
```

不要把上面的示例地址直接当成真实地址使用。

### 主控接口要求

主控至少需要兼容以下接口：

```text
GET  /api/health
GET  /api/workflow/list
GET  /api/workflow/config/{id}
POST /api/workflow/generate
GET  /api/workflow/result?prompt_id=...
POST /api/comfy/upload/file
```

可选接口包括 ComfyUI 状态、队列、GPU、硬件信息和 SSH 性能相关接口。详细字段以你的主控平台 API 文档为准。

## 五、节点如何工作

1. 打开左侧“节点库”。
2. 应用请求主控的 `/api/workflow/list`。
3. 节点库显示当前账号可用的工作流。
4. 点击某个工作流时，应用请求 `/api/workflow/config/{id}`。
5. 主控返回启用参数、默认值、字段标签和工作流模板。
6. 应用创建通用 `apiNode` 并自动生成参数表单。
7. 执行时提交该工作流的 `workflow_id`、用户参数和上游节点的图片/视频/音频输入。

因此，开源版不要求所有人拥有相同的节点。主控平台才是节点和工作流的实际来源。

### 节点刷新注意事项

- 新增工作流：刷新节点库即可看到。
- 删除工作流：刷新后不会再出现在服务器目录中，但旧画布中的节点仍可能保留原 `workflow_id`。
- 修改工作流参数：重新添加节点，或清除该节点的旧参数后重新读取配置。
- 主控暂时离线：应用可能使用本地缓存/内置配置显示旧节点，但不能保证这些节点仍能执行。
- 不要把自己的工作流目录文件直接复制给其他用户，除非其中不含私有地址、密钥和模型信息。

## 六、开发运行

### 只运行前端开发模式

```powershell
cd E:\AIhuabu\JaceCanvas\开源\desktop-app
npm run dev
```

### 直接运行桌面应用

先构建前端和本地服务端：

```powershell
cd E:\AIhuabu\JaceCanvas\开源\desktop-app
npm run build:web

cd ..\server
npm run build

cd ..\desktop-app
New-Item -ItemType Directory -Force server | Out-Null
Copy-Item ..\server\dist\* server -Recurse -Force
npm start
```

也可以在桌面端目录双击 `start-desktop.bat`，但它要求 `dist` 和内嵌 `server` 已经准备好。

## 七、生成 Windows 安装包

推荐直接运行：

```powershell
cd E:\AIhuabu\JaceCanvas\开源\desktop-app
\.\build-desktop.bat
```

脚本会完成：

1. 构建 React/Vite 前端。
2. 构建 `server` TypeScript 服务端。
3. 复制服务端构建产物到桌面端内嵌目录。
4. 复制图标。
5. 使用 electron-builder 生成 Windows x64 安装包。

安装包会输出到 `desktop-app/release-v3.11.6/`。这个目录属于构建产物，不要提交到开源仓库。

如果只想生成未安装的目录版：

```powershell
cd E:\AIhuabu\JaceCanvas\开源\desktop-app
npm run pack
```

## 八、开源发布前检查

- [ ] 删除 `desktop-app/config/prompt-settings.json`，只保留 `.example.json`
- [ ] 搜索并删除真实 API Key、Token、SSH 密码和私有域名
- [ ] 不提交 `node_modules`、`dist`、`release-*`、数据库、日志和上传素材
- [ ] 不提交个人工作流 JSON、主控导出文件和调试文档
- [ ] 用自己的主控地址刷新节点库
- [ ] 测试新增、删除和修改工作流
- [ ] 测试主控不可用时应用是否能正常启动并提示错误
- [ ] 在没有 Node.js 的干净 Windows 环境安装测试安装包
- [ ] 检查安装包中没有个人配置和个人服务地址

## 九、安全注意事项

- API Key 不要写进源码、README、批处理文件或工作流 JSON。
- SSH 密码只在应用设置中填写；不要通过截图、日志或文档发布。
- 公共仓库中不要保存真实服务器地址，示例统一使用 `example.com`。
- 主控 API 应使用 HTTPS，并在服务端验证权限和请求参数。
- 不要把主控接口开放为允许任意来源、任意工作流执行的公共服务。
- 工作流可能消耗 GPU 资源，主控端应设置鉴权、限流、任务超时和文件大小限制。

## 十、常见问题

### 节点库为空

检查 API 地址是否填写为服务根地址，网络是否可达，API Key 是否正确，然后点击刷新。也可以直接访问主控的 `/api/workflow/list` 检查返回结果。

### 能看到节点但参数为空

检查 `/api/workflow/config/{workflow_id}` 是否返回 `success: true`、`workflow_template` 和 `api_config`。工作流 ID 必须与主控返回的 ID 完全一致。

### 节点能添加但执行失败

检查主控的 `/api/workflow/generate` 参数格式、文件上传接口、工作流模板和 ComfyUI 服务状态。不同主控的字段定义可能不同。

### 本地后端没有启动

确认已执行 `npm run build` 生成 `server/dist`，并已将其复制到 `desktop-app/server`。同时检查 3001 端口是否被其他程序占用。
