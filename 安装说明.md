# JaceCanvas Windows 安装与使用说明

> 适用于安装版 **JaceCanvas 4.6.8**。本文面向 Windows 安装版用户。源码运行、二次开发和自行打包请查看 [`开源使用说明.md`](./开源使用说明.md)。

## 一、三个版本怎么选

| 版本 | 内容 | 适合 |
|------|------|------|
| **纯安装版** | 应用本体（无内置源码），体积约 450MB | 普通用户，快速安装 |
| **安装+内置开源** | 应用 + `resources/opensource` 完整源码（含 node_modules），约 1.7GB | 需要应用内 DevAgent 改码的用户 |
| **纯开源源码** | 完整源码 zip（不含 node_modules） | 开发者二次开发 |

- **百度网盘（推荐，国内速度快）：** <https://pan.baidu.com/s/1eVCgaoPqYvxneCNpcIb1bg?pwd=3buf>（提取码 `3buf`）
- **GitHub Releases：** <https://github.com/Nicke000/JaceCanvas/releases>

## 二、安装步骤

1. 下载对应版本的 `JaceCanvas Setup 4.6.8.exe`。
2. 双击运行，按向导选择安装目录（可自定义）。
3. 安装完成，桌面生成 JaceCanvas 图标。

> 安装版**数据目录独立**（`%APPDATA%\JaceCanvas`），与开发版互不干扰；重装/覆盖安装不残留旧数据。

## 三、首次使用

1. **连接服务器**：设置 → 服务器管理 → 添加服务器。
   - 本地 ComfyUI 填 `http://127.0.0.1:8188`。
   - **主控服务器**：节点/生成用主地址；如果性能检测不显示，在「性能检测地址」填另一个链接（例如 seetacloud 平台：节点地址 `https://uu...westd.seetacloud.com:8443`，性能地址 `https://u...westd.seetacloud.com:8443`，两个分开填）。
2. **配置 AI**：设置 → 聊天 AI（聊天用）/ 提示词 AI（分镜与翻译用）。
   - 短剧工作室内的「分镜 AI 设置」是独立配置，不影响聊天 AI。
3. **回到干净状态**：设置 → 关于 → 「清空所有本地数据」。

## 四、常见问题

- **有节点但性能检测空白？** 服务器编辑里补填「性能检测地址」（见上）。
- **聊天/分镜 AI 调不通？** 确认设置里 baseUrl/apiKey 已填并**正常退出应用**保存（强杀进程会丢配置）；分镜 AI 未单独配置会自动回退聊天 AI。
- **数据和设置在哪？** 安装版 `%APPDATA%\JaceCanvas`；旧版数据在 `%APPDATA%i-canvas-desktop`（新版本不再读取）。
- **打不开/闪退？** 更新显卡驱动；持续崩溃可查看崩溃日志（设置 → 日志）。

## 五、许可证

MIT License。第三方组件保留各自许可证（见 [`THIRD_PARTY_NOTICES.md`](./THIRD_PARTY_NOTICES.md) 与 [`开源使用说明.md`](./开源使用说明.md)）。
