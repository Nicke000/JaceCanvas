# JaceCanvas Windows 安装与使用说明

> 适用于安装版 **JaceCanvas 4.6.8**。本文面向 Windows 安装版用户。源码运行、二次开发和自行打包请查看 [`开源使用说明.md`](./开源使用说明.md)。

> **兼容性说明：** 当前安装版主要基于 **Zealman 大神的 ComfyUI 主控平台 API** 开发和验证。不保证兼容其他主控平台；使用其他主控需要自主完成 API 适配和二次开发。

## 图标入口

<p>
  <a href="https://github.com/Nicke000/JaceCanvas"><img src="https://img.shields.io/badge/Source-GitHub-181717?logo=github" alt="GitHub 源码"></a>
  <a href="https://qm.qq.com/q/ZFvPlgPQmm"><img src="https://img.shields.io/badge/QQ-交流群-12B7F5?logo=tencentqq&logoColor=white" alt="QQ 交流群"></a>
  <a href="https://www.facebook.com/share/1C2aPnT9N3/?mibextid=wwXIfr"><img src="https://img.shields.io/badge/Facebook-Contact-1877F2?logo=facebook&logoColor=white" alt="Facebook 联系我们"></a>
  <a href="mailto:mikesill701@gmail.com"><img src="https://img.shields.io/badge/Email-Contact-EA4335?logo=gmail&logoColor=white" alt="Gmail"></a>
  <a href="mailto:qiangzh49@outlook.com"><img src="https://img.shields.io/badge/Email-Contact-EA4335?logo=gmail&logoColor=white" alt="Outlook"></a>
</p>

## 下载安装包

- **百度网盘（推荐，国内速度快）：** <https://pan.baidu.com/s/1eVCgaoPqYvxneCNpcIb1bg?pwd=3buf>（提取码 `3buf`）
- **GitHub Releases：** <https://github.com/Nicke000/JaceCanvas/releases>（Windows x64 安装包 `JaceCanvas Setup 4.6.8.exe`）


1. 下载 Release 中的 `JaceCanvas Setup *.exe`。
2. 如 Windows SmartScreen 提示未识别发布者，请先核对 Release 校验值和发布来源；未签名版本不会自动获得微软发布者认证。
3. 选择安装目录并完成安装。
4. 首次进入后，在设置中填写你自己的主控 API 地址；不要把 API Key、SSH 密码或私有地址写入公开文档。

## 首次配置

启动后打开“设置”，按需填写以下项目：

| 配置项 | 是否必需 | 说明 |
|---|---:|---|
| 主控 API 地址 | 是 | 填写主控服务根地址，不要附加 `/api` 或具体接口路径 |
| API Key | 按主控要求 | 仅在主控启用鉴权时填写 |
| ComfyUI 地址 | 否 | 用于打开或测试原生 ComfyUI 页面 |
| SSH 主机、端口、用户、密码 | 否 | 仅用于远程性能检测 |

主控 API 至少需要提供工作流目录、工作流配置、生成、结果查询和文件上传接口。每个用户的节点和模型由自己的主控返回，安装包不绑定个人服务器。

## 外观与主题（4.6.8 新增）

- 一键切换 8 套主题：深空紫、海洋青、森林绿、暖橙、明亮白、樱花粉、石墨灰、赛博霓虹，并可自定义完整颜色；
- 打开设置 → **外观**，可实时调整圆角大小、界面密度、动画效果、毛玻璃、背景氛围光、字体大小、画布网格样式和强调色强度；
- 主题切换、节点执行状态、面板进出均带有平滑动画，界面配色全局跟随主题。

## 付费 API 节点（可选）

除主控工作流节点外，软件还提供付费 API 节点，按「图片 / 视频 / 管理 / 通用」分类，支持在不同厂商之间切换。

1. 打开 **设置 → 付费 API 厂商配置**，选择厂商并填写 API Key（部分厂商还需填写接口地址或区域）；
2. 在左侧 **创作工作区 → 节点 → 付费 API** 中按能力添加节点（如文生图、图生视频、对口型、主体管理、音色管理等）；
3. 在节点内选择厂商和模型（模型列表来自对应厂商的开放接口），填写提示词并连接所需输入。

> 不同厂商支持的能力和参数可能不同，不支持的能力不会显示；部分能力需要厂商平台提供对应的接口与模型，具体以厂商服务为准。

## SSH 性能检测

性能检测是可选功能，不影响普通工作流使用。桌面端会在本机寻找 Python 3.x，并使用 Paramiko 建立 SSH 连接；不会把 Python 或密码上传到第三方服务。

### Windows 本机要求

如果状态栏提示找不到 Python 或 Paramiko，请安装 Python 3.x，并在 PowerShell 执行：

```powershell
python -m pip install paramiko
```

应用也支持 Windows `py.exe -3` 启动器。若仍失败，可设置环境变量 `AI_CANVAS_PYTHON` 指向实际的 `python.exe` 路径后重新启动应用。

### 远程服务器要求

确保 SSH 端口可访问、账号允许密码登录，并根据需要安装：

```text
nvidia-smi   # GPU 信息，可选
top          # CPU 信息，可选
free         # 内存信息，可选
df           # 磁盘信息，可选
```

不要将 SSH 密码发送给开发者、写入文档或截图。若连接失败，请先检查地址、端口、防火墙和服务器 SSH 配置。

## Video2X 超分/补帧节点

打开左侧节点库，添加 **Video2X 超分/补帧（开源本地）** 节点。它支持：

- 视频超分：2x / 3x / 4x；
- 视频补帧：2 倍或 4 倍帧率；
- 超分 + 补帧。

可以从“选择本地视频”载入视频，也可以连接画布上游视频节点。处理结果保存在本机应用数据目录，并可连接到预览或后续节点。具体模型与运行库以随附的第三方许可证声明为准。

### 重要协议与版权说明

Video2X、RealESRGAN、RealCUGAN、RIFE、FFmpeg、Qt、NCNN、Vulkan 等不是 JaceCanvas 自研内容，各自遵守上游许可证。节点上的“开源本地模型”提示表示来源和运行方式，不表示所有组件拥有相同许可证，也不代表适用于所有电脑或获得商业授权。

请自行确认：

- 输入视频和输出内容的版权、肖像、隐私与商业使用权；
- 使用的具体模型文件和运行库的许可证；
- 显卡驱动、Vulkan 环境和本机硬件兼容性。

禁止侵权使用，禁止删除或伪造第三方版权声明，禁止把第三方程序或模型误标为 JaceCanvas 自研模型。来源和协议清单见 [`THIRD_PARTY_NOTICES.md`](./THIRD_PARTY_NOTICES.md)。

## 源码修改与二次开发

JaceCanvas 自有画布源码、节点 UI、导演台宿主和通用适配代码以 MIT License 开放。所有人可以修改、复制、发布和二次开发画布，包括个人、研究和商业用途；需要保留 MIT 版权/许可声明，并同时遵守第三方组件许可证。

## 故障排除

- **节点库为空**：检查主控 API 根地址、网络和鉴权设置。
- **Video2X 无法运行**：确认使用的是带本地 Video2X 资源的 Release，并更新显卡驱动/Vulkan runtime。
- **显存不足或处理很慢**：降低放大倍数、选择较轻模型、关闭其他 GPU 程序或改用远程工作流。
- **启动动画后界面异常**：尝试重新启动；如果显卡驱动不兼容，可在设置中关闭 GPU 加速。